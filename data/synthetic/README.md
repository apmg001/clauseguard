# Synthetic data

Phase 1 builds objects in code (see `scripts/run_demo.py`). As you progress,
drop generated contract/invoice fixtures and the **seeded-error** evaluation
set here (BUILD_BRIEF.md §8). Keep a manifest of planted errors so the eval
harness can compute detection recall/precision.
